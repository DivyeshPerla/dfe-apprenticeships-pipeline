.venv/
.git/
data/
docs/
terraform/
notebooks/
workflows/
__pycache__/
*.pyc
# Leading slash scopes this to the context root only. A bare `*.csv` also
# matches nested paths, which silently emptied dbt/seeds/ and broke the
# containerised build -- and a `!dbt/seeds/*.csv` negation did not override it.
/*.csv
dbt/target/
dbt/dbt_packages/
dbt/logs/
